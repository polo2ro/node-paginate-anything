{
  "name": "node-paginate-anything",
  "description": "Node server side for the angular-paginate-anything directive. Modify the http response for pagination, return 2 properties to use in a query",
  "version": "0.0.1",
  "private": false,
  "scripts": {
	"test": "jasmine-node spec/"
  },
  "dependencies": {
  },
  "devDependencies": {
    "jasmine-node": "~1.14.3"
  },
  "engine": {
    "node": ">=0.6"
  }
}
